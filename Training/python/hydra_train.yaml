hydra:
  run:
    dir: outputs/${experiment_name}/DeepPi_v1/${now:%Y-%m-%d_%H-%M-%S}
defaults:
  - ../configs@_global_.training_cfg: training
  - _self_

# mlflow
path_to_mlflow: mlruns
experiment_name: ???

pretrained: null
# pretrained:
#   run_id : deb4d6b122414f3cae0a4d0927b0765c #4d938a098fbb46898a368928c05091f7 
#   experiment_id: 7 #5
#   starting_model: DeepPi_v1_step_final.tf

# setup
gpu_cfg:
  gpu_mem  : 6.5 # in Gb
  gpu_index: 0
  
  