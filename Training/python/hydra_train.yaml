hydra:
  run:
    dir: outputs/${experiment_name}/${training_cfg.Setup.model_name}/${now:%Y-%m-%d_%H-%M-%S}
defaults:
  - ../configs@_global_.training_cfg: training
  - _self_

# mlflow
path_to_mlflow: mlruns
experiment_name: ???

pretrained: null
# pretrained:
#   run_id : 6daf836007814378b081099af6f7b671 
#   experiment_id: 10
#   starting_model: DeepPi_v1_step_final.tf

# setup
gpu_cfg:
  gpu_mem  : 6.5 # in Gb
  gpu_index: 0
  
  